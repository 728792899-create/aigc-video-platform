# AIGC 辅助的短视频创意生成与制作平台

## 环境要求

- Node.js >= 18.x（推荐 20+）
- npm >= 9.x
- FFmpeg（需加入系统 PATH，用于视频合成导出）

## 快速启动

### 1. 安装依赖

```bash
# 根目录安装（Electron 相关，可选）
npm install

# 安装后端依赖
cd server
npm install

# 安装前端依赖
cd ../client
npm install
```

### 2. 配置环境变量

```bash
# 后端配置
cd server
cp .env.example .env
# 编辑 .env，填入你自己的 DeepSeek API Key
```

### 3. 启动后端

```bash
cd server
node app.js
# 默认监听 http://localhost:3000
```

### 4. 启动前端（开发模式）

```bash
cd client
npm run dev
# 默认监听 http://localhost:5173，自动代理 /api 到后端 3000 端口
```

### 5. 访问平台

浏览器打开 http://localhost:5173 即可使用。

## 项目结构

```
├── client/          # 前端 Vue 3 + Element Plus + Vite
├── server/          # 后端 Node.js + Express + SQLite
│   ├── routes/      # API 路由
│   ├── services/    # 业务服务（AI调用、TTS、视频合成）
│   ├── db/          # 数据库模块
│   └── .env.example # 环境变量模板
├── electron/        # Electron 桌面应用配置
├── build/           # 构建资源
├── docs/            # 文档
├── scripts/         # 辅助脚本
└── README.md
```

## 技术栈

- 前端：Vue 3 + Element Plus + Pinia + Vue Router + Vite
- 后端：Node.js + Express + sql.js（SQLite）
- AI 服务：DeepSeek（文案生成）、Pollinations.ai（图片生成）、Edge TTS（语音合成）
- 视频合成：FFmpeg

## 注意事项

- 首次启动后端会自动创建 SQLite 数据库文件
- uploads/ 目录用于存储生成的图片和音频资源
- 如需更换 AI 模型，修改 server/.env 中的配置即可
