# AIGC 视频工作台启动说明

本页提供最短启动路径。完整介绍见 [README](README.md)，常见错误见[故障排查](docs/troubleshooting.md)。

## 环境

- Node.js 22 或更高版本；
- npm 10 或更高版本；
- macOS、Windows 或常见 Linux；
- 无需单独安装 FFmpeg，仓库默认使用 `ffmpeg-static`。

## 安装

在仓库根目录执行：

~~~bash
npm ci
npm --prefix server ci
npm --prefix client ci
~~~

三个目录都有独立 lockfile，不要只安装根依赖。

## 推荐：无 Key Demo

~~~bash
npm run demo
~~~

打开终端打印的地址，通常是 `http://127.0.0.1:5173`。端口被占用时启动器会选择其他可用端口。

Demo Mode：

- 使用本地模板生成脚本；
- 使用原创占位画面；
- 使用本地占位音轨；
- 本地生成字幕和时间线；
- 真实调用 FFmpeg 输出可播放 MP4；
- 不调用付费 Provider。

不需要复制 `.env`，也不要为了体验 Demo 填写真实 API Key。

## 验收

~~~bash
npm run test:smoke
~~~

测试在系统临时目录创建数据库和媒体，验证：

1. 无 Key 全流程导出；
2. export 阶段失败后只重试该阶段；
3. 服务停止并重启后从检查点续跑；
4. 最终文件是有效 MP4。

## 分开启动

需要调试时使用两个终端：

~~~bash
npm --prefix server start
~~~

~~~bash
npm --prefix client run dev
~~~

默认后端地址是 `http://127.0.0.1:3000`，Vite 默认地址是 `http://127.0.0.1:5173`。

## Electron 开发模式

先确保依赖已安装，然后：

~~~bash
DEMO_MODE=1 DEMO_SILENT_TTS=1 npm run electron:dev
~~~

桌面版使用 Electron 用户数据目录保存数据库、媒体、日志和受系统保护的凭证。公开截图和测试应使用隔离数据目录或纯 Demo 数据。

## Provider 配置

只有主动使用云端能力时才需要 Provider 凭证。优先在 Electron 设置页中保存，让系统 Keychain/Credential Manager 负责持久化。

安全要求：

- 不把 Key 写入 README、截图或提交；
- 不在前端 `.env` 中保存服务器密钥；
- 不把真实 `settings.json` 或 `credentials.vault` 上传到 Issue；
- 配置导出和数据库备份不包含密钥；
- 测试始终使用无 Key Demo 或受控假服务。

## 常用命令

~~~bash
npm run quality
npm run test:smoke
node scripts/security-check.mjs
node scripts/ffmpeg-smoke.mjs
npm run electron:preflight
~~~

桌面打包和签名见[桌面发布指南](docs/desktop-release.md)。
